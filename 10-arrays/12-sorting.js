const names = ['Ahmad', 'Omar', 'bob', 'Baraa', 'Eldeirawi'];

// Sort() sorts string value in order from uppercase then does the lowercase
console.log(names.sort());



const numbers = [ 1, 40, 15, 20, 2];

// Built in JS function used to see if (x - y) is positive or negtive

numbers.sort((x, y) => x - y);

console.log(numbers);