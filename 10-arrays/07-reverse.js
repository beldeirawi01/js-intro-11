const numbers = [1, 2, 3];

console.log(numbers.reverse()); // Outputs [3, 2, 1]

console.log(numbers); // Still outputs [3, 2, 1]


console.log(numbers.reverse()); // reverses array again, leading to it outputing the original array

